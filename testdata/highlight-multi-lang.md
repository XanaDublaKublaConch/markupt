# Multiple fenced languages in one document

```python
def hello(name):
    return f"Hello, {name}!"
```

Some prose in between.

```cpp
int add(int a, int b) {
    return a + b;
}
```

```mermaid
graph TD
  A --> B
```
